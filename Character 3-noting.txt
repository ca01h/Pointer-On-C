1.一个无符号变量对比相同长度的有符号变量不会容纳更大的值：
	数在内存中都是以二进制形式来存储，只是输出的时候会有是否有符号之别。

2.假如int和float型都是32位长，int类型所能容纳的值的精度更大一些。
	对于float型，相同的数值会有多种表示方法。

3.static用于函数定义时，或者用于代码块之外的变量声明时，static用于修改标识符的链接属性，从external改为internal，但存储类型和作用域不改变。
  用于代码块内部的变量声明时，static用于修改变量的存储类型，从自动变量修改为静态变量，但变量的链接属性和作用域不受影响。